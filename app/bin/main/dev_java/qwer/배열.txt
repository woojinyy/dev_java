클래스  new 클래스()
주서번지 간접 참조 방식
전역변수의 스코프 ,지역변수의 스코프?
초기화 ㄱㄴ?
변수-> 한번에 하나만 같은타입끼리
배열은 끼워넣기가 안된다 사이즈 변경 불가// 한번에 여러개 담을 수 있다
객체 배열끼워넣기가 안된다 사이즈 변경 불가// 다른 타입 담을 수 있다.

클래스 -기능클래스      main, 호출(재사용성)
          -데이터 클래스   x    , get(read) ,set(write) method //  데이터의 영속성 > MySQL ,오라클

배열 생성
-원시형 타입 int is[]; 
                        for();
                    int is2[i][j]
                        for(i){
                            for(j){

                            }
                        }
-객체 배열